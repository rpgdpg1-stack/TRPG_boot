-- Индекс users(total_muscles DESC) строился под сортировку рейтинга/лиг.
-- Соревновательную часть из продукта убрали (лиги, лидерборды, ранги), сортировки
-- по этой колонке в коде не осталось — индекс только занимал место и добавлял
-- работы на каждой записи прогресса.
--
-- САМА КОЛОНКА total_muscles ОСТАЁТСЯ: это валюта личного прогресса, её читают
-- и пишут (api_finish_workout, api_reset_my_progress, профиль). Удалён только
-- порядок сортировки по убыванию, который нужен был для «топа».
-- Применено на проде 2026-08-08.

DROP INDEX IF EXISTS public.idx_users_total_muscles;
