/**
 * Отладочный лог, который молчит в проде.
 *
 * В сборке было 29 `console.log` с полезной для разработки телеметрией —
 * очередь синка, авторизация, состояние сети, payload завершённой тренировки.
 * В проде это лишний шум в консоли Telegram и утечка данных пользователя
 * (в логах светились id и содержимое операций).
 *
 * `console.warn` и `console.error` НЕ трогаем: они сообщают о реальных
 * проблемах и нужны в проде — по ним ловятся баги у живых пользователей.
 */
const ENABLED = import.meta.env.DEV

export function debug(...args) {
  if (ENABLED) console.log(...args)
}
