/**
 * Ракета — знак режима «Быстрая тренировка».
 *
 * `lit` — режим включён: ракета красится акцентом, под ней загорается шлейф.
 * Погашенная — тёмно-серая, как любой выключенный тумблер в проекте.
 *
 * Форма своя (не Material): у материаловской ракеты слишком много деталей,
 * на 20px они схлопываются в пятно. Здесь корпус-капля, два крыла и окно —
 * силуэт читается и мелким.
 */
export default function RocketIcon({ size = 20, lit = false, color }) {
  const tint = color || (lit ? 'var(--color-primary)' : 'var(--color-text-secondary)')

  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      aria-hidden="true"
      style={{ display: 'block', flexShrink: 0 }}
    >
      {/* Шлейф — только у включённой: у выключенной ракета «стоит на земле». */}
      {lit && (
        <path
          d="M9.6 16.6 7.4 20.9c-.2.4.2.8.6.6l4.2-2.2"
          stroke="var(--color-streak)"
          strokeWidth="2"
          strokeLinecap="round"
          opacity="0.9"
        />
      )}
      {/* Корпус: нос вверх-вправо, как у взлетающей. */}
      <path
        d="M13.3 4.1c2.6-1.4 5.3-1.5 6.3-1.4.5.1.9.5 1 1 .1 1-.1 3.7-1.4 6.3-1 2-2.6 3.7-4.2 5l-2.5 2-3.6-3.6 2-2.5c1.3-1.6 3-3.2 5-4.2"
        fill={tint}
      />
      {/* Крылья — по обе стороны корпуса. */}
      <path
        d="M8.9 6.6 6 7.4c-.5.1-.9.5-1 1L4.6 11l3.3-1.1zM17 15.2l-.8 2.9c-.1.5-.5.9-1 1l-2.5.4 1.1-3.3z"
        fill={tint}
        opacity="0.75"
      />
      {/* Окно. */}
      <circle cx="15.4" cy="8.6" r="1.7" fill="var(--bg-base)" />
    </svg>
  )
}
